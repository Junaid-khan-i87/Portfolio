// This component is deprecated. Scroll-to-top logic is now handled by the 
// PageWrapper component in App.tsx using Framer Motion's onAnimationComplete callback,
// which ensures scrolling occurs after page transitions.
const ScrollToTop = () => {
  return null;
};

export default ScrollToTop;